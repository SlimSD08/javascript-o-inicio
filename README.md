# javascript - O Início
Estudos iniciais de JavaScript

----

## Conteúdo Inicial

- item 1
- item 2
- item 3
# Conteúdo Mínimo para Começar a Estudar JavaScript

## 1. Introdução ao JavaScript
- O que é JavaScript e para que serve  
- Como incluir JavaScript em uma página HTML

## 2. Conceitos Básicos
- Sintaxe básica  
- Comentários  
- Variáveis (`var`, `let`, `const`)  
- Tipos de dados (`string`, `number`, `boolean`, `null`, `undefined`, `object`)

## 3. Operadores
- Operadores aritméticos (`+`, `-`, `*`, `/`, `%`)  
- Operadores de comparação (`==`, `===`, `!=`, `!==`, `>`, `<`, `>=`, `<=`)  
- Operadores lógicos (`&&`, `||`, `!`)  
- Operadores de atribuição (`=`, `+=`, `-=`, etc.)

## 4. Estruturas de Controle
- Condicionais (`if`, `else if`, `else`, `switch`)  
- Laços de repetição (`for`, `while`, `do...while`)

## 5. Funções
- Declaração de funções  
- Parâmetros e retorno  
- Funções anônimas e arrow functions

## 6. Arrays
- Criação e manipulação básica  
- Métodos comuns (`push`, `pop`, `shift`, `unshift`, `forEach`, `map`, `filter`)

## 7. Objetos
- Definição e propriedades  
- Acessar e modificar propriedades  
- Métodos de objetos

## 8. Manipulação do DOM (básico)
- Selecionar elementos (`getElementById`, `querySelector`)  
- Modificar conteúdo e estilo  
- Eventos básicos (`click`, `input`, `submit`)

## 9. Noções de Escopo e Hoisting
- Escopo global vs local  
- Diferença entre `var`, `let` e `const`

## 10. Conceitos Básicos de Programação Assíncrona
- Callbacks  
- Promises (introdução)